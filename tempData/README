# For debugging purposes

If the flag `writeAppDataToTempFiles` is set to `true`, this directory will be filled with nicely formatted JSON files representing any recently saved app data.

The JSON files will be named after the owner's username & the app name

  e.g. `${formattedUsername}-${appName}.json`